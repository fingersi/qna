.comments(data-commentable-type=commentable.class.name data-commentable-id=commentable.id)
  table
    tbody
      = render partial: "comments/comment",
        collection: commentable.comments,
        as: :comment,
        cached: true